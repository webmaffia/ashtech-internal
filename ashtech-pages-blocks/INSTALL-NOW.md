# ⚡ INSTALL NOW - 3 Commands

## 🎯 Your Plugin Uses edit.js + save.js

**Perfect for frequently updating content!**

All content is stored in WordPress database and editable through the editor.

---

## 🚀 Installation Commands

### **Copy and paste these 3 commands:**

```bash
# 1. Navigate to plugin folder
cd D:\Server\htdocs\ashtech-internal\ashtech-pages-blocks

# 2. Install dependencies
npm install

# 3. Build blocks
npm run build
```

**That's it!** ✨

---

## ✅ What This Does

### **Command 1:** `npm install`
- Downloads WordPress packages
- Installs build tools
- Sets up dependencies
- **Time:** ~2 minutes

### **Command 2:** `npm run build`
- Compiles all 9 blocks
- Creates `build/` folder
- Generates JavaScript files
- **Time:** ~1 minute

---

## 📦 After Build

**You'll have:**

```
ashtech-pages-blocks/
├── build/                    ← NEW FOLDER
│   ├── home-page/index.js
│   ├── project-page/index.js
│   ├── contact-page/index.js
│   └── (6 more blocks)
│
├── home-page/
│   ├── edit.js               ← Editor interface
│   ├── save.js               ← Your complete HTML
│   └── index.js              ← Registration
│
└── (8 other blocks)
```

---

## ✅ Then Activate

```
WordPress Admin → Plugins → "Ashtech Pages Blocks" → Activate
```

---

## 🎯 How to Use

### **Insert Block:**
```
Pages → Add New → (+) → "Contact Page" → Insert
```

### **Edit Content:**
```
Click any field → Type → Auto-saves to database ✅
```

### **Publish:**
```
Click "Publish" → Page shows with your complete HTML ✅
```

---

## 💡 Key Advantage

### **Content Updates (NO BUILD):**

```
Edit in WordPress → Update → Done!
```

**No code changes, no npm build, no deployment!**

Content is in database, so anyone can update it! ✅

---

## 🎯 When to Build

**Build is only needed when:**
- ❌ First installation
- ❌ Adding new editable fields
- ❌ Changing HTML structure in save.js

**Build NOT needed for:**
- ✅ Editing content in WordPress
- ✅ Changing text
- ✅ Uploading images
- ✅ Daily content updates

---

## 🎉 Summary

**Run these 2 commands:**

```bash
npm install
npm run build
```

**Then activate plugin and start editing content!**

**All your content will be in WordPress database and easily editable!** ✨

---

## 🚀 Start Now

```bash
cd ashtech-pages-blocks
npm install && npm run build
```

**Then activate in WordPress!** 🎉

